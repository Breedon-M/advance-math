const addNumbers = function(a,b) {
  return a+b;
};


const subtractNumbers = function(a,b) {
  return a-b;
};


const divFunc = function(a,b){
  return a / b;
}
//  in the file math.js add a function to divide two numbers at 
//the end of the file. the function should take two numbers as arguments
// (e.g., a and b) and return the result of their division (e.g. a/b)


console.log(divFunc(5,5));
console.log(addNumbers());

console.log(subtractNumbers());

